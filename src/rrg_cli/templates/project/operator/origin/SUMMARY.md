# Held-back origin summary

Transcribe the origin report's per-question methods and findings here, one section
per question, using the **original** question numbering. The scorecard and the
Origin tab read these `## Q<n>` headings to grade each question on its own, so every
question needs its own section. Values must be transcribed from the report, not
recomputed.

Replace the stub sections below with real content (and add one per question).

## Q1 - <topic>

Method: <how the analysis was done>. Finding: <result as stated in the report>.
