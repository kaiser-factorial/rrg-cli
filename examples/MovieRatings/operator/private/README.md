# Operator-private source material

`ORIGINAL_SPEC.pdf` is the original course assignment. It contains methodological
hints, so the neutral study overview and questions are shared instead. The PDF is
retained here for provenance and must never enter a validator package.
